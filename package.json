{
  "name": "discord-sentinel",
  "version": "1.0.0",
  "description": "A robust and lightweight bot used to manage and secure discord guilds.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist && rm -rf ./data/error.log && rm -rf ./data/debug.log",
    "build": "tsc",
    "start": "node ./dist/index.js > ./data/debug.log 2> ./data/error.log"
  },
  "author": "SeqSEE",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.3.1",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@types/ws": "^7.2.6"
  }
}
